'use client';

export const Header = ({
  currentLocale,
  allLocales,
  navigationLinks,
}: ILayout) => {

  return (
    <header className={'sticky left-0 top-0 z-20'}>
      
    </header>
  );
};
